<h3>关注我的（<%= @followers.count %>）</h3>

<% @followers.each do |follow| %>
  <%= link_to image_tag("normal_icon.jpeg", :size => "60x60",:class => "img-thumbnail")%>
  <%= link_to follow.name, follow %>
<% end %>
<%= will_paginate @followers ,:previous_label =>"上一页",:next_label=>"下一页" %>